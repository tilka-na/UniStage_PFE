server:
  port: 8081
spring:
  datasource:
    url: jdbc:postgresql://${AUTH_DB_HOST:localhost}:5432/auth_db
    username: postgres
    password: 1234

  mail:
    host: smtp.gmail.com
    port: 587
    username: nadia.arrahime@gmail.com
    password: bxew adnw injk hugm //i mean this
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ""
            client-secret: ""
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  application:
    name: AUTH-SERVICE

eureka:
  client:
    service-url:
      defaultZone: http://${EUREKA_HOST:localhost}:8761/eureka