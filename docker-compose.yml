version: "3.8"

services:
  postgres:
    image: postgres:15
    container_name: unistage-postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 1234
      POSTGRES_DB: unistage
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  mongodb:
    image: mongo:6
    container_name: unistage-mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db

  zookeeper:
    image: confluentinc/cp-zookeeper:7.5.0
    container_name: unistage-zookeeper
    environment:
      ZOOKEEPER_CLIENT_PORT: 2181
    ports:
      - "2181:2181"

  kafka:
    image: confluentinc/cp-kafka:7.5.0
    container_name: unistage-kafka
    depends_on:
      - zookeeper
    ports:
      - "9092:9092"
    environment:
      KAFKA_BROKER_ID: 1
      KAFKA_ZOOKEEPER_CONNECT: zookeeper:2181
      KAFKA_LISTENERS: PLAINTEXT://0.0.0.0:9092
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://kafka:9092
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1

  eureka-server:
    container_name: unistage-eureka
    build: ./eureka-server
    ports:
      - "8761:8761"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8761/actuator/health"]
      interval: 10s
      timeout: 5s
      retries: 5

  auth-service:
    container_name: unistage-auth
    build: ./auth-service
    depends_on:
      postgres:
        condition: service_started
      eureka-server:
        condition: service_healthy # Ghadi itsenna Eureka t-welli Healthy
    environment:
      DB_HOST: postgres
      EUREKA_HOST: eureka-server
    ports:
      - "8081:8081"

  profile-service:
    container_name: unistage-profile
    build: ./profile-service
    depends_on:
      postgres:
        condition: service_started
      eureka-server:
        condition: service_started
    environment:
      DB_HOST: postgres
      EUREKA_HOST: eureka-server
    ports:
      - "8082:8082"

  internship-service:
    container_name: unistage-internship
    build: ./internship-service
    depends_on:
      postgres:
        condition: service_started
      kafka:
        condition: service_started
      eureka-server:
        condition: service_healthy
    environment:
      DB_HOST: postgres
      KAFKA_HOST: kafka
      EUREKA_HOST: eureka-server
    ports:
      - "8083:8083"

  document-service:
    container_name: unistage-document
    build: ./document-service
    depends_on:
      mongodb:
        condition: service_started
      eureka-server:
        condition: service_healthy
    environment:
      MONGO_HOST: mongodb
      EUREKA_HOST: eureka-server
    ports:
      - "8084:8084"

  notification-service:
    container_name: unistage-notification
    build: ./notification-service
    depends_on:
      kafka:
        condition: service_started
      mongodb:
        condition: service_started
      eureka-server:
        condition: service_healthy
    environment:
      KAFKA_HOST: kafka
      MONGO_HOST: mongodb
      EUREKA_HOST: eureka-server
    ports:
      - "8085:8085"

  api-gateway:
    container_name: unistage-gateway
    build: ./api-gateway
    depends_on:
      eureka-server:
        condition: service_healthy
    environment:
      EUREKA_HOST: eureka-server
    ports:
      - "8080:8080"

  frontend:
    container_name: unistage-frontend
    build: ./unistage-ui
    ports:
      - "4200:80"

volumes:
  postgres_data:
  mongo_data: