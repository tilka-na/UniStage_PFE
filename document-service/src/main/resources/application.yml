server:
  port: 8084
spring:
  application:
    name: DOCUMENT-SERVICE
  mongodb:
    uri: mongodb://${MONGO_HOST:mongodb}:27017/documentdb
eureka:
  client:
    service-url:
      defaultZone: http://${EUREKA_HOST:eureka-server}:8761/eureka