server:
  port: 8084
spring:
  application:
    name: DOCUMENT-SERVICE
  mongodb:
    uri: mongodb://${MONGO_HOST:localhost}:27017/documentdb

eureka:
  client:
    service-url:
      defaultZone: http://${EUREKA_HOST:localhost}:8761/eureka
